# دليل ربط نظام الحضور والغياب بـ Google Sheets 📊

هذا الدليل سيشرح لك كيفية جعل النظام يعمل "أونلاين" بحيث يتم تخزين البيانات في Google Sheets ومشاركتها بين المدير والمعلمين.

---

## الخطوة 1: إعداد Google Sheet

1. اذهب إلى [Google Sheets](https://sheets.google.com) وقم بإنشاء **جدول بيانات جديد** (Blank Spreadsheet).
2. قم بتسمية الملف باسم واضح، مثلاً: `قاعدة_بيانات_المدرسة`.
3. لا تحتاج لإنشاء أي أعمدة، السكربت سيقوم بذلك تلقائياً.

---
SSSS
## الخطوة 2: إضافة كود Apps Script

1. من داخل ملف Google Sheet، اضغط على القائمة **Extensions** (الإضافات) ثم اختر **Apps Script**.
2. سيفتح لك تبويب جديد يحتوي على محرر أكواد.
3. امسح أي كود موجود في المحرر (عادةً تجد `function myFunction() {...}`).
4. افتح الملف المرفق `كود_الربط_Google_Script.txt` وانسخ محتواه بالكامل.S
5. الصق الكود في محرر Apps Script.
6. اضغط على أيقونة الحفظ 💾 (أو Ctrl+S) وسمّ المشروع مثلاً `SchoolScript`.

---

## الخطوة 3: نشر السكربت (Deploy)

هذه الخطوة مهمة جداً للحصول على الرابط:

1. اضغط على زر **Deploy** (نشر) الأزرق في أعلى اليمين، ثم اختر **New deployment**.
2. اضغط على الترس ⚙️ بجانب "Select type" واختر **Web app**.
3. املأ البيانات كالتالي:
   - **Description**: أي وصف (مثلاً: v1).
   - **Execute as**: اجعلها **Me** (بريدك الإلكتروني).
   - **Who has access**: **مهم جداً:** اختر **Anyone** (أي شخص).
4. اضغط **Deploy**.
5. سيطلب منك "Authorize access" (منح الصلاحيات). اضغط عليه، اختر حسابك، ثم اضغط **Advanced** (إعدادات متقدمة) ثم **Go to SchoolScript (unsafe)** ثم **Allow**.
6. سيظهر لك رابط طويل تحت عنوان **Web app URL**.
7. **انسخ هذا الرابط**.

---

## الخطوة 4: ربط النظام

1. افتح ملف `نظام_اونلاين.html` باستخدام أي محرر نصوص (Notepad مثلاً).
2. ابحث في الأسفل عن السطر:
   ```javascript
   const SCRIPT_URL = "ضع_الرابط_هنا";
   ```
3. استبدل عبارة `ضع_الرابط_هنا` بالرابط الذي نسخته من الخطوة السابقة.
   - تأكد أن الرابط بين علامتي التنصيص `""`.
4. احفظ الملف.

---

## الخطوة 5: التشغيل

1. افتح `نظام_اونلاين.html` في المتصفح.
2. سيظهر لك "جاري الاتصال بقاعدة البيانات...".
3. بمجرد الاتصال، سيقوم السكربت تلقائياً بإنشاء الصفحات (Students, Teachers, Absences) في ملف Google Sheet الخاص بك.
4. **اسم المستخدم الافتراضي:** `admin`
5. **كلمة المرور الافتراضية:** `1234`

---

## ملاحظات هامة

*   **الطلاب:** عند تشغيل النظام لأول مرة، ستكون قائمة الطلاب فارغة. يجب عليك استيراد ملف Excel/CSV للطلاب من لوحة تحكم المدير، أو إضافتهم يدوياً في صفحة "Students" في Google Sheet.
*   **المعلمين:** يمكنك إضافة المعلمين من لوحة التحكم، وسيظهرون فوراً في صفحة "Teachers" في Google Sheet.
*   **الأمان:** الرابط الذي حصلت عليه يمنح صلاحية القراءة والكتابة في هذا الملف المحدد فقط. لا تشاركه مع الطلاب، فقط مع المعلمين والمدير.
