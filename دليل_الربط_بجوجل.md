# دليل ربط النظام بـ Google Sheets

لجعل ملف Google Sheets يعمل كـ "سيرفر" للنظام، يرجى اتباع الخطوات التالية بدقة:

## 1. إعداد ملف Google Sheets
1. افتح ملف Google Sheets الخاص بك (الذي يحتوي على بيانات الطلاب).
2. تأكد أن الورقة الأولى تحتوي على بيانات الطلاب مع الأعمدة التي ذكرتها (A-Z).
3. قم بإنشاء ورقة جديدة فارغة وسمّها **"الغيابات"** (سيتم تخزين الغيابات هنا).

## 2. إضافة الكود البرمجي (Apps Script)
1. في ملف Google Sheets، اضغط على **الإضافات (Extensions)** > **Apps Script**.
2. سيفتح لك محرر أكواد. امسح أي كود موجود.
3. انسخ الكود الموجود في ملف `google_apps_script.js` (المرفق مع النظام) والصقه هناك بالكامل.
4. اضغط على أيقونة الحفظ (💾).

## 3. نشر السكربت (Deploy)
1. اضغط على زر **Deploy** (نشر) الأزرق في الأعلى > **New deployment** (نشر جديد).
2. بجانب "Select type" اضغط على الترس ⚙️ واختر **Web app**.
3. املأ البيانات كالتالي:
   - **Description**: نظام الحضور
   - **Execute as**: Me (أنا)
   - **Who has access**: **Anyone** (أي شخص) - *هذا مهم جداً لكي يعمل النظام*.
4. اضغط **Deploy**.
5. سيطلب منك "Authorize access" (منح الصلاحيات). اضغط عليه، اختر حسابك، ثم Advanced > Go to (Project Name) (unsafe) > Allow.
6. سيظهر لك رابط **Web App URL**. انسخ هذا الرابط.

## 4. ربط النظام
1. افتح ملف `نظام_مع_z` باستخدام محرر نصوص (Notepad).
2. ابحث عن السطر:
   `const SCRIPT_URL = 'ضع_رابط_Web_App_هنا';`
3. استبدل `ضع_رابط_Web_App_هنا` بالرابط الذي نسخته في الخطوة السابقة.
4. احفظ الملف.

الآن يمكنك إرسال ملف `نظام_مع_google_sheets.html` للمعلمين، وسيعمل النظام ويرتبط مباشرة بملف الإكسل (Google Sheets) الخاص بك!
